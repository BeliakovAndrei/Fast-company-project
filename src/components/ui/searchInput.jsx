import React from "react";

const SearchInput = () => {
    return (
        <SearchInput
            className="mb-3"
            type="text"
            placeholder="Поиск по имени..."
        />
    );
};

export default SearchInput;
